server:
  port: 8080

spring:
  application:
    name: parro-talk-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/parrotalk}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    enabled: ${FLYWAY_ENABLED:false}
    baseline-on-migrate: true
    baseline-version: 20260517.03
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}

  ai:
    model:
      chat: openai
    openai:
      api-key: ${GROQ_API_KEY:}
      base-url: ${GROQ_BASE_URL:https://api.groq.com/openai/v1}
      chat:
        options:
          model: ${GROQ_MODEL:llama-3.3-70b-versatile}
          temperature: 0.2

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - email
              - profile

  # SMTP has been replaced by Resend API
  # mail:
  #   host: ${MAIL_HOST:localhost}

app:
  upload:
    dir: uploads
  python-service:
    url: http://localhost:8000/upload
  cloudinary:
    cloud_name: ${CLOUDINARY_CLOUD_NAME}
    api_key: ${CLOUDINARY_API_KEY}
    api_secret: ${CLOUDINARY_API_SECRET}

email:
  provider: resend
  from: ${APP_MAIL_NAME:ParroTalk} <${APP_MAIL_FROM:noreply@parrotalk.local}>
  reply-to: ${APP_SUPPORT_EMAIL:support@parrotalk.local}
  resend:
    api-key: ${RESEND_API_KEY:INSERT_SECRET_HERE}
    base-url: ${RESEND_BASE_URL:https://api.resend.com}

application:
  frontend:
    base-url: ${FRONTEND_BASE_URL:http://localhost:3000}
    allowed-origins: ${FRONTEND_ALLOWED_ORIGINS:http://localhost:3000}
  email-verification:
    token-expiration-minutes: ${EMAIL_VERIFICATION_TOKEN_EXPIRATION_MINUTES:30}
    resend-cooldown-seconds: ${EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS:60}
    metadata-retention-minutes: ${EMAIL_VERIFICATION_METADATA_RETENTION_MINUTES:1440}
  password-reset:
    token-expiration-minutes: ${PASSWORD_RESET_TOKEN_EXPIRATION_MINUTES:30}
    max-requests-per-hour: ${PASSWORD_RESET_MAX_REQUESTS_PER_HOUR:5}
  security:
    public-endpoints: /api/auth/register,/api/auth/login,/api/auth/refresh,/api/auth/verify-email,/api/auth/resend-verification-email,/api/auth/forgot-password,/api/auth/reset-password,/api/auth/reset-password/verify
    jwt:
      secret-key: ${JWT_SECRET_KEY}
      expiration: 86400000
      refresh-token:
        expiration: 604800000

settings:
  default-time-zone: Asia/Bangkok
